.DS_Store
autosaves/
*.kir-cache/
target/
.claude/
# The Flatpak build (release.yml) stages the built bundle and its work dirs
# here; none of it is committed.
packaging/flatpak/bundle/
.flatpak-builder/
flatpak-build/
flatpak-repo/


# The OFX support library writes this beside whatever ran a plugin — a bare
# log of suite fetches and actions, no use to anybody after the run that made
# it. Running the host from flutter_ui/ leaves one there.
ofxTestLog.txt
