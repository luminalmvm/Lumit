// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These functions are ignored because they are not marked as `pub`: `colour_of`, `linear_of`, `text_document_of`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`

/// A colour as the document stores it: scene-linear RGBA, which may exceed 1
/// (an HDR tint) or dip below 0 (a lift), so it is not a byte triple.
class BridgeColourRgba {
  final double r;
  final double g;
  final double b;
  final double a;

  const BridgeColourRgba({
    required this.r,
    required this.g,
    required this.b,
    required this.a,
  });

  @override
  int get hashCode => r.hashCode ^ g.hashCode ^ b.hashCode ^ a.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeColourRgba &&
          runtimeType == other.runtimeType &&
          r == other.r &&
          g == other.g &&
          b == other.b &&
          a == other.a;
}

/// A solid asset's definition.
class BridgeSolidDef {
  final String name;
  final BridgeColourRgba colour;
  final int width;
  final int height;

  const BridgeSolidDef({
    required this.name,
    required this.colour,
    required this.width,
    required this.height,
  });

  @override
  int get hashCode =>
      name.hashCode ^ colour.hashCode ^ width.hashCode ^ height.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeSolidDef &&
          runtimeType == other.runtimeType &&
          name == other.name &&
          colour == other.colour &&
          width == other.width &&
          height == other.height;
}

/// A text layer's document (v1: one styled run — docs/03 §9.1).
class BridgeTextDocument {
  final String text;

  /// When set, the layer's words come from this expression at each frame
  /// rather than from `text`, which is kept so switching the expression off
  /// restores what was typed.
  final String? expression;

  /// Pixel size at natural scale.
  final double size;
  final BridgeColourRgba fill;

  const BridgeTextDocument({
    required this.text,
    this.expression,
    required this.size,
    required this.fill,
  });

  @override
  int get hashCode =>
      text.hashCode ^ expression.hashCode ^ size.hashCode ^ fill.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeTextDocument &&
          runtimeType == other.runtimeType &&
          text == other.text &&
          expression == other.expression &&
          size == other.size &&
          fill == other.fill;
}
