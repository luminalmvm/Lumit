// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'package:uuid/uuid.dart';
import 'project.dart';

// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `from`, `from`

/// One blank in a reason's sentence, by name: `ae_mode` → `Dissolve`,
/// `percent` → `50`. Named rather than positional so a translation may put
/// them in whatever order its language wants.
class BridgeImportArg {
  final String name;
  final String value;

  const BridgeImportArg({
    required this.name,
    required this.value,
  });

  @override
  int get hashCode => name.hashCode ^ value.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeImportArg &&
          runtimeType == other.runtimeType &&
          name == other.name &&
          value == other.value;
}

/// The four grades of docs/11 §9, as the panel's filter sees them.
enum BridgeImportOutcome {
  imported,
  adjusted,
  placeholder,
  skipped,
  ;
}

/// Everything the import has to say: docs/11 §9's four counts, and the rows
/// beneath them.
class BridgeImportReport {
  final int imported;
  final int adjusted;
  final int placeholders;
  final int skipped;
  final List<BridgeImportRow> rows;

  const BridgeImportReport({
    required this.imported,
    required this.adjusted,
    required this.placeholders,
    required this.skipped,
    required this.rows,
  });

  @override
  int get hashCode =>
      imported.hashCode ^
      adjusted.hashCode ^
      placeholders.hashCode ^
      skipped.hashCode ^
      rows.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeImportReport &&
          runtimeType == other.runtimeType &&
          imported == other.imported &&
          adjusted == other.adjusted &&
          placeholders == other.placeholders &&
          skipped == other.skipped &&
          rows == other.rows;
}

/// One line of the report (docs/11 §9): where it happened, what grade the
/// outcome got, and why.
class BridgeImportRow {
  /// The item, as a reader would say it — "Main ▸ clip.mp4 ▸ Position". The
  /// user's own words (comp and layer names), so it is not translated.
  final String path;
  final BridgeImportOutcome outcome;

  /// The stable id of the sentence to write — see the module note.
  final String reason;
  final List<BridgeImportArg> args;

  /// The engine's own English, for a frontend with no sentence for `reason`.
  final String english;

  const BridgeImportRow({
    required this.path,
    required this.outcome,
    required this.reason,
    required this.args,
    required this.english,
  });

  @override
  int get hashCode =>
      path.hashCode ^
      outcome.hashCode ^
      reason.hashCode ^
      args.hashCode ^
      english.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeImportRow &&
          runtimeType == other.runtimeType &&
          path == other.path &&
          outcome == other.outcome &&
          reason == other.reason &&
          args == other.args &&
          english == other.english;
}

/// The imported project and its report, together, because a caller that got one
/// without the other would have to ask twice for two halves of one answer.
class BridgeImportedProject {
  final ProjectReference project;
  final BridgeImportReport report;

  const BridgeImportedProject({
    required this.project,
    required this.report,
  });

  @override
  int get hashCode => project.hashCode ^ report.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeImportedProject &&
          runtimeType == other.runtimeType &&
          project == other.project &&
          report == other.report;
}
