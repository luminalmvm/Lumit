// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These functions are ignored because they are not marked as `pub`: `drain_to_newest`, `publish_frame`, `render_comp_with_preview`, `render_comp`, `trace_scope`, `worker_loop`
// These types are ignored because they are neither used by any `pub` functions nor (for structs and enums) marked `#[frb(unignore)]`: `RenderCompRequestWithPreview`, `RenderCompRequest`, `RenderScopeRequest`, `WorkerRequest`, `WorkerState`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_fields_are_eq`, `clone`, `eq`, `fmt`
// These functions are ignored (category: IgnoreBecauseExplicitAttribute): `run_worker`

/// How playback should behave when the machine cannot render at the composition's
/// own rate — the choice the Viewer offers and shows (docs/13 §B5).
enum BridgePlaybackMode {
  /// Keep time, lower the resolution. The realtime controller measures each
  /// frame and drops to a coarser preview tier until playback keeps up, so the
  /// picture stays in step with the sound and gets softer instead of stuttering.
  /// Frames are not kept: they are of a tier that may change from one second to
  /// the next, and filing them would leave the cache full of pictures at
  /// resolutions nobody asked for.
  adaptive,

  /// Every frame at the resolution asked for, however long it takes, and kept.
  /// Playback runs as fast as the renderer manages rather than on the clock —
  /// so the caller pauses the sound, because sound that cannot keep time is
  /// worse than none.
  everyFrame,
  ;
}
