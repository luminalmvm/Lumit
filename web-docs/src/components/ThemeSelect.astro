---
// Nothing. The site has one ramp - the application's, dark (docs/15-DESIGN.md,
// K-004) - exactly as web/ does, so Starlight's dark/light/auto picker had no
// second theme to pick and is removed rather than left as a control that does
// nothing. theme.css pins the tokens on plain `:root`, so the ramp holds even
// for a visitor whose browser still remembers `starlight-theme: light`.
---
