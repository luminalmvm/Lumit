---
title: Brightness
description: "Brighten or darken the image."
---

Brightness is a [Colour](/effects/colour/) effect.

{/* GENERATED:example - rewritten by `npm run docs:effects`. Edit the prose, not this block. */}

<figure class="example">
  <div class="compare" style="--split:50%">
    <img src="/effects/plate.webp" alt="The frame before Brightness is applied" width="1280" height="544" loading="lazy" decoding="async" />
    <img class="compare__after" src="/effects/colour/brightness.webp"
      alt="The same frame with Brightness applied" width="1280" height="544" loading="lazy" decoding="async" />
    <span class="compare__handle" aria-hidden="true"></span>
    <span class="compare__label compare__label--a" aria-hidden="true">Original</span>
    <span class="compare__label compare__label--b" aria-hidden="true">Brightness</span>
    <input class="compare__range" type="range" min="0" max="100" step="0.1" value="50"
      aria-label="Reveal Brightness. Left of the handle is the untouched frame, right of it is the effect." />
  </div>
  <figcaption>Drag the handle. Brightness is on the right, the untouched frame on the left,
  at settings chosen to make the effect plain rather than subtle.</figcaption>
</figure>

{/* END GENERATED */}

## What it does

Adds or subtracts light to every pixel.

## Parameters

{/* GENERATED:parameters - rewritten by `npm run docs:effects`. Edit the prose, not this block. */}

| Parameter | Control | Range | Default | Unit |
| --- | --- | --- | --- | --- |
| **Brightness** | Slider | -100 to 100; any value by typing | 0 | - |
| **Contrast** | Slider | -100 to 100; higher by typing | 0 | - |
| **Mix** | Slider | 0 to 100 | 100 | - |
| **Matte** | Layer | Any layer in the composition | None | - |
| **Invert** | Checkbox | On or off | Off | - |

In the Effect Controls panel:

- **Matte** scales how much of the effect each pixel gets: white applies it in full, black leaves the pixel as it arrived, grey part way. **Invert** reads the matte the other way round, light for dark.

{/* END GENERATED */}

## What each control does

- **Brightness** is the light added to every channel. At full deflection either
  way it is a whole unit of scene-linear light, which is a lot; small numbers
  are the usual ones.
- **Contrast** is the spread about mid-grey. Zero passes the picture through and
  −100 flattens the frame to grey. At +100 the spread doubles, and there is no
  ceiling above.
- **Mix**: how much of the result replaces the original.
- **Matte** and **Invert** drive the brightness amount from another layer's brightness; see [Effects](/use/effects/).

## Notes

Brightness is addition, so it lifts the shadows as much as highlights. That makes 
it flatter than [Exposure](/effects/colour/exposure/), which multiplies instead. 

## Related

- [Contrast](/effects/colour/contrast/)
- [Exposure](/effects/colour/exposure/)
- [Levels](/effects/colour/levels/)
- [Colour](/effects/colour/)
- [Effects](/use/effects/)
