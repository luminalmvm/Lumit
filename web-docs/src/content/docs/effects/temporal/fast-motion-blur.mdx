---
title: "Fast motion blur"
description: "Smear each pixel along motion in footage."
---

Fast motion blur is a [Temporal](/effects/temporal/) effect.

{/* GENERATED:example - rewritten by `npm run docs:effects`. Edit the prose, not this block. */}

<figure class="example">
  <div class="compare" style="--split:50%">
    <img src="/effects/plate.webp" alt="The frame before Fast motion blur is applied" width="1280" height="544" loading="lazy" decoding="async" />
    <img class="compare__after" src="/effects/temporal/fast-motion-blur.webp"
      alt="The same frame with Fast motion blur applied" width="1280" height="544" loading="lazy" decoding="async" />
    <span class="compare__handle" aria-hidden="true"></span>
    <span class="compare__label compare__label--a" aria-hidden="true">Original</span>
    <span class="compare__label compare__label--b" aria-hidden="true">Fast motion blur</span>
    <input class="compare__range" type="range" min="0" max="100" step="0.1" value="50"
      aria-label="Reveal Fast motion blur. Left of the handle is the untouched frame, right of it is the effect." />
  </div>
  <figcaption>Drag the handle. Fast motion blur is on the right, the untouched frame on the left,
  at settings chosen to make the effect plain rather than subtle.</figcaption>
</figure>

{/* END GENERATED */}

## What it does

This effect measures where every pixel moved between one frame and the next, and
smears each one along its own path. 

:::caution[One flow measurement per layer]
Measuring motion is expensive. A layer computes it once per frame and shares it,
so where a layer carries both this and
[Datamosh](/effects/distortion/datamosh/), whichever comes first in the stack
gets the measurement and the other does nothing. Put them on separate layers if
you need both.
:::

## Parameters

{/* GENERATED:parameters - rewritten by `npm run docs:effects`. Edit the prose, not this block. */}

| Parameter | Control | Range | Default | Unit |
| --- | --- | --- | --- | --- |
| **Shutter angle** | Slider | 0 to 720 | 180 | - |
| **Samples** | Slider | 8 to 32; down to 2 and up to 64 by typing | 16 | - |
| **Quality** | Dropdown | Normal · High | Normal | - |
| **View** | Dropdown | Rendered · Motion vectors · Confidence · Dominant motion | Rendered | - |
| **Mix** | Slider | 0 to 100 | 100 | - |
| **Matte** | Layer | Any layer in the composition | None | - |
| **Invert** | Checkbox | On or off | Off | - |

In the Effect Controls panel:

- **Matte** scales how much of the effect each pixel gets: white applies it in full, black leaves the pixel as it arrived, grey part way. **Invert** reads the matte the other way round, light for dark.

{/* END GENERATED */}

## What each control does

- **Shutter angle** sets how long the imaginary shutter is open.
- **Samples** is a *maximum* on the number of samples the blur combines.
- **Quality**: increasing gives higher quality blur, for increased render time.
- **View** is a diagnostic display. Motion vectors show measured movement
  as colour. Confidence shows where the measurement is likely correct in white, and
  Dominant motion shows the neighbourhood motion an uncertain pixel borrows.
- **Mix**: how much of the result replaces the original.
- **Matte** and **Invert** drive the strength of the effect from another layer's brightness; see [Effects](/use/effects/).

## Related

- [Motion blur](/effects/temporal/motion-blur/)
- [Echo](/effects/temporal/echo/)
- [Directional blur](/effects/blur-sharpen/directional-blur/)
- [Retime](/use/retime/)
- [Temporal](/effects/temporal/)
- [Effects](/use/effects/)
