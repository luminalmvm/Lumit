---
title: "Sharpen"
description: "The simple, basic sharpen."
---

Sharpen is a [Blur & sharpen](/effects/blur-sharpen/) effect.

{/* GENERATED:example - rewritten by `npm run docs:effects`. Edit the prose, not this block. */}

<figure class="example">
  <div class="compare" style="--split:50%">
    <img src="/effects/plate.webp" alt="The frame before Sharpen is applied" width="1280" height="544" loading="lazy" decoding="async" />
    <img class="compare__after" src="/effects/blur-sharpen/sharpen.webp"
      alt="The same frame with Sharpen applied" width="1280" height="544" loading="lazy" decoding="async" />
    <span class="compare__handle" aria-hidden="true"></span>
    <span class="compare__label compare__label--a" aria-hidden="true">Original</span>
    <span class="compare__label compare__label--b" aria-hidden="true">Sharpen</span>
    <input class="compare__range" type="range" min="0" max="100" step="0.1" value="50"
      aria-label="Reveal Sharpen. Left of the handle is the untouched frame, right of it is the effect." />
  </div>
  <figcaption>Drag the handle. Sharpen is on the right, the untouched frame on the left,
  at settings chosen to make the effect plain rather than subtle.</figcaption>
</figure>

{/* END GENERATED */}

## What it does

It exaggerates the difference between each pixel and its 
neighbours, which crisps the image.

## Parameters

{/* GENERATED:parameters - rewritten by `npm run docs:effects`. Edit the prose, not this block. */}

| Parameter | Control | Range | Default | Unit |
| --- | --- | --- | --- | --- |
| **Amount** | Slider | 0 to 5; higher by typing | 1 | - |
| **Radius** | Slider | 1 to 8; higher by typing | 1 | - |
| **Mix** | Slider | 0 to 100 | 100 | - |
| **Matte** | Layer | Any layer in the composition | None | - |
| **Invert** | Checkbox | On or off | Off | - |

In the Effect Controls panel:

- **Matte** scales how much of the effect each pixel gets: white applies it in full, black leaves the pixel as it arrived, grey part way. **Invert** reads the matte the other way round, light for dark.

{/* END GENERATED */}

## What each control does

- **Amount** changes the amount the difference from neighbours is exaggerated.
- **Radius** is how far out the four neighbours are taken.
- **Mix**: how much of the result replaces the original.
## Related

- [Unsharp mask](/effects/blur-sharpen/unsharp-mask/)
- [Gaussian blur](/effects/blur-sharpen/gaussian-blur/)
- [Blur & sharpen](/effects/blur-sharpen/)
- [Effects](/use/effects/)
