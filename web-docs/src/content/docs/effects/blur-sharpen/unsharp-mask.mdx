---
title: "Unsharp mask"
description: "A radius-controlled detail lift."
---

Unsharp mask is a [Blur & sharpen](/effects/blur-sharpen/) effect.

{/* GENERATED:example - rewritten by `npm run docs:effects`. Edit the prose, not this block. */}

<figure class="example">
  <div class="compare" style="--split:50%">
    <img src="/effects/plate.webp" alt="The frame before Unsharp mask is applied" width="1280" height="544" loading="lazy" decoding="async" />
    <img class="compare__after" src="/effects/blur-sharpen/unsharp-mask.webp"
      alt="The same frame with Unsharp mask applied" width="1280" height="544" loading="lazy" decoding="async" />
    <span class="compare__handle" aria-hidden="true"></span>
    <span class="compare__label compare__label--a" aria-hidden="true">Original</span>
    <span class="compare__label compare__label--b" aria-hidden="true">Unsharp mask</span>
    <input class="compare__range" type="range" min="0" max="100" step="0.1" value="50"
      aria-label="Reveal Unsharp mask. Left of the handle is the untouched frame, right of it is the effect." />
  </div>
  <figcaption>Drag the handle. Unsharp mask is on the right, the untouched frame on the left,
  at settings chosen to make the effect plain rather than subtle.</figcaption>
</figure>

{/* END GENERATED */}

## What it does

The picture is compared against a blurred copy of itself and the difference is added back.

## Parameters

{/* GENERATED:parameters - rewritten by `npm run docs:effects`. Edit the prose, not this block. */}

| Parameter | Control | Range | Default | Unit |
| --- | --- | --- | --- | --- |
| **Amount** | Slider | 0 to 300 | 60 | - |
| **Radius** | Slider | 0.05 to 2; down to 0 and up to 4 by typing | 0.4 | Per cent of the composition diagonal |
| **Threshold** | Slider | 0 to 1 | 0.05 | - |
| **Luminance only** | Checkbox | On or off | On | - |
| **Mix** | Slider | 0 to 100 | 100 | - |
| **Matte** | Layer | Any layer in the composition | None | - |
| **Invert** | Checkbox | On or off | Off | - |

In the Effect Controls panel:

- **Matte** scales how much of the effect each pixel gets: white applies it in full, black leaves the pixel as it arrived, grey part way. **Invert** reads the matte the other way round, light for dark.

{/* END GENERATED */}

## What each control does

- **Amount** sets how strongly recovered detail is added back.
- **Radius** is the size of detail that gets lifted.
- **Threshold** is how different a pixel must be to be lifted.
- **Luminance only** sharpens brightness while leaving colour alone.
- **Mix**: how much of the result replaces the original.

## Related

- [Sharpen](/effects/blur-sharpen/sharpen/)
- [Gaussian blur](/effects/blur-sharpen/gaussian-blur/)
- [Retime](/use/retime/)
- [Blur & sharpen](/effects/blur-sharpen/)
- [Effects](/use/effects/)
