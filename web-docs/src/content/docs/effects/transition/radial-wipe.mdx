---
title: Radial wipe
description: "Sweeps a wedge around a centre."
---

Radial wipe is a **Transition** effect.

{/* GENERATED:example - rewritten by `npm run docs:effects`. Edit the prose, not this block. */}

<figure class="example">
  <div class="compare" style="--split:50%">
    <img src="/effects/plate.webp" alt="The frame before Radial wipe is applied" width="1280" height="544" loading="lazy" decoding="async" />
    <img class="compare__after" src="/effects/transition/radial-wipe.webp"
      alt="The same frame with Radial wipe applied" width="1280" height="544" loading="lazy" decoding="async" />
    <span class="compare__handle" aria-hidden="true"></span>
    <span class="compare__label compare__label--a" aria-hidden="true">Original</span>
    <span class="compare__label compare__label--b" aria-hidden="true">Radial wipe</span>
    <input class="compare__range" type="range" min="0" max="100" step="0.1" value="50"
      aria-label="Reveal Radial wipe. Left of the handle is the untouched frame, right of it is the effect." />
  </div>
  <figcaption>Drag the handle. Radial wipe is on the right, the untouched frame on the left,
  at settings chosen to make the effect plain rather than subtle.</figcaption>
</figure>

{/* END GENERATED */}

## What it does

Wipes the frame around a centre point, like a clock.

## Parameters

{/* GENERATED:parameters - rewritten by `npm run docs:effects`. Edit the prose, not this block. */}

| Parameter | Control | Range | Default | Unit |
| --- | --- | --- | --- | --- |
| **Wipe centre x** | Slider | 0 to 3840; any value by typing | 960 | Pixels at composition size |
| **Wipe centre y** | Slider | 0 to 2160; any value by typing | 540 | Pixels at composition size |
| **Completion** | Slider | 0 to 100 | 50 | - |
| **Start angle** | Dial | Any angle; snaps every 15° while a modifier is held | 0° | - |
| **Wipe** | Dropdown | Clockwise · Anticlockwise · Both | Clockwise | - |
| **Feather** | Slider | 0 to 500; higher by typing | 0 | Pixels at composition size |
| **Mix** | Slider | 0 to 100 | 100 | - |
| **Matte** | Layer | Any layer in the composition | None | - |
| **Invert** | Checkbox | On or off | Off | - |

In the Effect Controls panel:

- **Matte** scales how much of the effect each pixel gets: white applies it in full, black leaves the pixel as it arrived, grey part way. **Invert** reads the matte the other way round, light for dark.

{/* END GENERATED */}

## What each control does

- **Wipe centre x** and **Wipe centre y** positions the wipe's centre.
- **Completion** is how far the sweep is as a precentage.
- **Start angle** is the direction  the sweep begins from.
- **Wipe** chooses which way the wedge opens.
- **Feather** softens the sweeping edge.
- **Mix**: how much of the result replaces the original.
- **Matte** and **Invert** drive the strength of the effect from another layer's brightness; see [Effects](/use/effects/).

## Related

- [Linear wipe](/effects/transition/linear-wipe/)
- [Set matte](/effects/utility/set-matte/)
- [Transition](/effects/transition/)
- [Effects](/use/effects/)
