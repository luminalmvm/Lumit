---
title: "Datamosh"
description: "Melt frames together."
---

Datamosh is a [Distortion](/effects/distortion/) effect.

{/* GENERATED:example - rewritten by `npm run docs:effects`. Edit the prose, not this block. */}

<figure class="example">
  <div class="compare" style="--split:50%">
    <img src="/effects/plate.webp" alt="The frame before Datamosh is applied" width="1280" height="544" loading="lazy" decoding="async" />
    <img class="compare__after" src="/effects/distortion/datamosh.webp"
      alt="The same frame with Datamosh applied" width="1280" height="544" loading="lazy" decoding="async" />
    <span class="compare__handle" aria-hidden="true"></span>
    <span class="compare__label compare__label--a" aria-hidden="true">Original</span>
    <span class="compare__label compare__label--b" aria-hidden="true">Datamosh</span>
    <input class="compare__range" type="range" min="0" max="100" step="0.1" value="50"
      aria-label="Reveal Datamosh. Left of the handle is the untouched frame, right of it is the effect." />
  </div>
  <figcaption>Drag the handle. Datamosh is on the right, the untouched frame on the left,
  at settings chosen to make the effect plain rather than subtle.</figcaption>
</figure>

{/* END GENERATED */}

## What it does

Imitates what a compressed video does when reference frames go missing.

The blurring/melting follows motion in the footage. Moving objects will smear and
static objects don't change at all.

:::caution[One flow measurement per layer]
Measuring motion is expensive, so a layer computes it once per frame and shares
it. If a layer somehow carries both this effect and
[Fast motion blur](/effects/temporal/fast-motion-blur/) at the same time,
whichever comes first in the stack gets the measurement and the other quietly
does nothing. Put them on separate layers if you need both.
:::

## Parameters

{/* GENERATED:parameters - rewritten by `npm run docs:effects`. Edit the prose, not this block. */}

| Parameter | Control | Range | Default | Unit |
| --- | --- | --- | --- | --- |
| **Intensity** | Slider | 0 to 1; higher by typing | 1 | - |
| **Displacement** | Slider | 1 to 16; higher by typing | 4 | - |
| **Bloom** | Slider | 0 to 1 | 0.6 | - |
| **Reset interval** | Slider | 0 to 5; higher by typing | 0 | - |
| **Mix** | Slider | 0 to 100 | 100 | - |
| **Matte** | Layer | Any layer in the composition | None | - |
| **Invert** | Checkbox | On or off | Off | - |

In the Effect Controls panel:

- **Matte** scales how much of the effect each pixel gets: white applies it in full, black leaves the pixel as it arrived, grey part way. **Invert** reads the matte the other way round, light for dark.

{/* END GENERATED */}

## What each control does

- **Intensity** is how much of the prediction is laid over the current frame.
- **Displacement** the number of frames used to warp across.
- **Bloom** affects the falloff of older frames being used in the datamosh.
- **Reset interval** is how often the melt restarts clean, immitating reference frames.
- **Mix**: how much of the result replaces the original.
- **Matte** and **Invert** drive the strength of the effect from another layer's brightness; see [Effects](/use/effects/).

## Related

- [Fast motion blur](/effects/temporal/fast-motion-blur/)
- [Echo](/effects/temporal/echo/)
- [Block glitch](/effects/distortion/block-glitch/)
- [Distortion](/effects/distortion/)
- [Effects](/use/effects/)
