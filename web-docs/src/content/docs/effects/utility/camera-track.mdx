---
title: Camera track
description: "Works out how the camera moved in a shot."
---

Camera track is a **Utility** effect.

{/* GENERATED:example - rewritten by `npm run docs:effects`. Edit the prose, not this block. */}



{/* END GENERATED */}

## What it does

Tracks the footage and allows you to recreate the camera path within Lumit. Apply it 
to a layer and press **Analyse**. You can keep editing while it runs. 
The whole source clip is analysed, so every clip using that footage shares the one
solve. Trimming, moving or retiming a clip doesn't track it again.

A track can also cover only part of a clip. Where the shot stops being followable —
a whiteout, a fast lens rack, a cut in the middle of the file — there is nothing
after that point that can be tied to anything before it, so the analysis stops there
and solves the part that worked. The bar above the status line shows how much of the
clip carries a camera, and the line says how far it got. A camera linked to a partial
track follows it inside that span and holds its last position past the end. Mask out
whatever broke the shot and analyse again, or treat the two halves as two shots.

## Parameters

{/* GENERATED:parameters - rewritten by `npm run docs:effects`. Edit the prose, not this block. */}

| Parameter | Control | Range | Default | Unit |
| --- | --- | --- | --- | --- |
| **Analyse** | Button | - | - | - |
| **Cancel** | Button | - | - | - |
| **Feature density** | Dropdown | Low · Normal · High | Normal | - |
| **Use masks** | Checkbox | On or off | On | - |
| **Show points** | Checkbox | On or off | On | - |

{/* END GENERATED */}

## What each control does

- **Analyse**: start tracking.
- **Cancel**: stop a track that's running.
- **Feature density**: how many points are tracked. More is slower but copes better
  with hard footage.
- **Use masks**: masks on the layer are left out of the track. Use this for anything
  moving on its own, like a person.
- **Show points**: draws the solved points over the picture. Select some to create a
  null or solid at their position.

## Related

- [Cameras and 3D](/use/camera/)
- [Masks](/use/masks/)
- [Utility](/effects/#utility)
- [Effects](/use/effects/)
